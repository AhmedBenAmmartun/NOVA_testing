aiohttp
python-dotenv
psutil

# Optional: only needed if you set SPOTIFY_CLIENT_ID/SPOTIFY_CLIENT_SECRET
# for the real Spotify Web API. Without it, Spotify state still works via
# the window-title fallback (Windows only).
spotipy

# Testing (not required to run the dashboard)
pytest
